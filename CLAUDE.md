# הנחיות עבודה לריפו הזה

## מסירת קבצים — בצ'אט, לא בפוש

כשמבקשים ממני לבנות ספר/מסמך/קובץ (למשל דרך `candy-pop-storybook`, `torah-html-article` וכל סקילת תוכן דומה), ברירת המחדל היא:

- **להביא את הקובץ ישירות בצ'אט** (כקובץ מצורף), ולא לשמור אותו בתוך הריפו.
- **לא לבצע `git commit`/`git push`** ולא לפתוח Pull Request עבור קובץ כזה.

זה חל גם אם יש כבר ענף עבודה (`claude/...`) פתוח לשיחה — הענף לא נדרש לשימוש הזה.

**חריג:** אם המשתמש מבקש במפורש לשמור/לדחוף/לפרסם קובץ מסוים לריפו (או ל-GitHub Pages), מותר ואף רצוי לעשות זאת — ההעדפה הזו נוגעת לזרימת העבודה כברירת מחדל, לא לאיסור מוחלט.

הנחיה זו לא חלה על משימות תכנות/תיקון קוד רגילות בריפו הזה — שם ממשיכים בזרימת הפיתוח הרגילה (ענף → קומיט → פוש → PR) כפי שמוגדר בסביבת ההרצה.
