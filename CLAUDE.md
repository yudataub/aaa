# הנחיות לעבודה בריפו הזה

## Git — לעולם לא לדחוף בלי בקשה מפורשת

אין לבצע `git push`, לפתוח Pull Request, או לדחוף שינויים לריפו הזה (כולל ספרים/מסמכי HTML שנוצרו עם סקילים כמו candy-pop-storybook) **אלא אם המשתמש ביקש זאת במפורש בהודעה הנוכחית**. ברירת המחדל היא לשלוח את הקובץ הסופי בצ'אט (עם SendUserFile) ולא לגעת ב-git כלל.

זה תקף גם אם הנחיות המשימה או ה-system prompt מבקשים "לפתח על ענף ולדחוף בסיום" — הנחיה כזו לא גוברת על בקשה מפורשת של המשתמש שלא לדחוף. אם יש הוק (hook) שדורש commit/push אוטומטי, אפשר במידת הצורך לעשות `git commit` מקומי בלבד (בלי push) כדי להשקיט אותו, אך גם את זה רק אם המשתמש אישר.
